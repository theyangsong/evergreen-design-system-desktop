---
description: Stroke-based SVG icons — non-scaling stroke when sizing with --icon-* tokens
globs: packages/**/*,apps/showcase/**/*
alwaysApply: false
---

# 描边型 SVG 放大：边框不随缩放变粗

## 问题

用 CSS 放大 SVG（例如 `width` / `height` 从 `--icon-md` 调到 `--icon-lg`）时，若图形用 **stroke 描边**绘制，描边会随图形一起 **同比变粗**，视觉上像「边框被强行放大」，与设计稿不符。

## 原则

- 描边宽度用 **Evergreen token**（如 `var(--stroke-lg)`、`calc(var(--stroke-xs) * 0.7)`），不要写死 px（除非 spec 明确要求且无 token）。
- 在 **实际绘制的节点**（`<path>`、`<line>`、`<circle>` 等）上设置 `vector-effect: non-scaling-stroke`。
- **`processSvg`** 会对 stroke 形状注入 `vector-effect="non-scaling-stroke"` presentation attribute，并 **移除** path 上内联 `stroke-width`（宽度单源：`var(--stroke-lg)` CSS，避免 Chrome attribute+CSS 双源歧义）。
- **`vector-effect` 不继承**：父级 `<svg>` 或 `<g>` 上设置无效；token 线稿的 `vector-effect` 只写在 SVG attribute，不在 `Icon.module.css` 重复。

## CSS 写法（推荐与模板一致）

```css
.myIcon {
  display: block;
  width: var(--icon-lg);
  height: var(--icon-lg);
  overflow: visible;
  color: var(--text-base-primary);
}

/* 仅适用于 stroke 图标：fill: none + stroke */
.myIcon :global(path),
.myIcon :global(line),
.myIcon :global(circle) {
  fill: none;
  stroke: currentColor;
  stroke-width: var(--stroke-lg);
  vector-effect: non-scaling-stroke;
}
```

## 适用范围

- **Desktop 组件**（`packages/components/**`）内随 `--icon-sm|md|lg` 缩放的 stroke 图标。
- Showcase **`.desktopTokens`** 预览区内的示例 SVG（与 live 组件一致，用 Desktop `--stroke-*` / `--icon-*`）。

## Toggle 控件外环（Checkbox / Radio / Decide）

Figma 为 **16×16 含 1.4 描边**。若用 `border: var(--stroke-lg)` + `box-sizing: border-box`，内容区会缩到约 13.2px，而 `EgIcon` 仍按 **16px + non-scaling 1.4px** 描边绘制，环与勾/点比例会偏离稿面。

**外环用 inset ring，不用 border 占内容区：**

```css
border: none;
box-shadow: inset 0 0 0 var(--stroke-lg) var(--stroke-base-quaternary);
```

选中/填充态去掉 `box-shadow`，仅保留背景色。

**不适用**：fill 实心图标、壳层 Website 装饰 SVG（除非同样用 stroke + token 缩放）。
