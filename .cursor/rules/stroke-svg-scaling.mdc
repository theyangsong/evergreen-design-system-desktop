---
description: Stroke-based SVG icons — non-scaling stroke when sizing with --icon-* tokens
globs: packages/**/*,apps/showcase/**/*
alwaysApply: false
---

# 描边型 SVG 放大

完整方案见 `packages/components/docs/icon-stroke-scaling.md`（对齐 **eds-doc** `renderIconHtml`）。

## EgIcon

- `sizeIconMarkup`：显式 `svg width/height` + attribute `stroke` / `stroke-width`
- **不用** CSS stroke、**不用** `vector-effect`
- 32 viewBox → displayPx：`stroke_user = 1.4 × 32 / displayPx`

## Showcase 手写 SVG

仍可用 `stroke-width: var(--stroke-lg)` + `vector-effect: non-scaling-stroke`（与 EgIcon 管线分离）。
