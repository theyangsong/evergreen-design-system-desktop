---
description: 滚动区顶/底毛玻璃遮罩（Top & Bottom Mask）— DOM 契约、状态机、token/glue；业务优先用 DS 组件
globs: packages/**/*,apps/showcase/**/*
alwaysApply: false
---

# Top & Bottom Mask — 滚动顶底毛玻璃遮罩

**场景：** 固定高度面板内纵向溢出滚动时，顶栏 / 底栏在内容滚过时叠 **半透明遮罩 + 背景模糊**，并可选显示分隔线（`EgDivider`）。

**不适用：**

| 场景 | 用 |
|------|-----|
| 全页 / Layout 主区滚动 | `.motion-page`、Layout `scrollPanel` |
| 浮层进出场毛玻璃 | `.effect-flotation-box` / `.effect-popup-box` + microFloat |
| Popover 面板本体 | `effect-popover-box`（见 `eds-project.mdc` §1 Popover） |
| 菜单标题常驻模糊 | `.eds-frosted-menu-chrome`（`frostedMenuChrome.css`） |

---

## 原则（锁死）

1. **单滚动容器**：顶栏、正文、底栏必须在 **同一个** `overflow-y: auto` 容器内；禁止 header 固定在外层、body 另滚。
2. **sticky 在滚动容器内**：顶 `position: sticky; top: 0`，底 `position: sticky; bottom: 0`，并设 `z-index`。
3. **毛玻璃 = 半透明 + blur**：`background: var(--effect-mask)` + `backdrop-filter: var(--eds-blur-bg)`；**禁止**在开启态使用不透明 `var(--box-page)` / hex。
4. **按滚动位置切换**：未滚到边界时顶/底可为实色；滚过阈值后再挂 mask class（见状态机）。
5. **高度链**：容器须 `height: 100%` + `min-height: 0`（flex 子项），否则不溢出 → 状态机永不触发。

---

## DOM 结构（定稿）

```text
.scrollPanel（唯一滚动容器 · overflow-y: auto · overscroll-behavior: contain）
├── header.chromeTop（sticky top: 0 · z-index: 1）
│   ├── .heading（标题 / 操作）
│   └── EgDivider（可选 · 滚离顶部后 show）
├── .body（正文 slot · flex: 1 0 auto）
└── footer.chromeBottom（sticky bottom: 0 · z-index: 1）
    └── action / 工具栏
```

正文从顶底栏 **下方穿过**；`backdrop-filter` 模糊的是 **同一滚动容器内正在移动的正文**。

---

## 视觉层（Token + Glue）

| 用途 | Token / Class |
|------|----------------|
| 半透明遮罩 | `var(--effect-mask)` — 浅色 `color(var(--eds-page) / .82)`（见 `packages/tokens/spec/color/semantic.json`） |
| 背景模糊 | `var(--eds-blur-bg)` — `saturate(360%) blur(50px)` |
| 全局 glue class | `.eds-frosted-page-chrome`（`packages/components/src/styles/frostedPageChrome.css`） |

```css
.eds-frosted-page-chrome {
  background: var(--effect-mask);
  -webkit-backdrop-filter: var(--eds-blur-bg);
  backdrop-filter: var(--eds-blur-bg);
}
```

**模块 CSS 冲突：** 若 sticky 层默认 `background: var(--box-page)`，须在挂 `.eds-frosted-page-chrome` 时用 **更高优先级** 覆盖为 `effect-mask`（参考 `Skid.module.css` `.title:global(.eds-frosted-page-chrome)`）。

---

## 状态机（滚动阈值）

常量：`SCROLL_EDGE_EPSILON = 2`（与 `apps/showcase/src/composables/scrollContainment.ts` 一致）。

```text
canScroll     = scrollHeight - clientHeight > ε
showTopMask   = canScroll && scrollTop > ε
showBottomMask = canScroll && scrollTop + clientHeight < scrollHeight - ε
```

| 滚动位置 | 顶栏 | 底栏 |
|----------|------|------|
| 不可滚动 | 实色 `--box-page` | 实色 |
| 顶部（未滚） | 实色 | mask（若可滚且未到底） |
| 中间 | mask + 顶部分隔线 | mask + 底部分隔线 |
| 底部 | mask | 实色 |

监听：`@scroll` + `ResizeObserver`（内容高度变化时重算）。参考：`packages/components/src/templates/skid/Skid.vue`。

---

## 两种实现路径

### A. 直接挂 class（Skid / ToolBar / Paginer）

滚动满足条件时给 sticky 节点加 `eds-frosted-page-chrome`。

- **参考**：`Skid.vue`（条件 class + `EgDivider :hide`）
- **ToolBar**：常驻 frosted（`ToolBar.vue`）
- **Paginer**：底栏常驻 frosted（`Paginer.vue`）

### B. 伪元素铺 mask（Popup sticky 工具栏）

避免与内容层 background 打架：`::before` 铺 `effect-mask` + blur，内容 `z-index: 1`。

- **参考**：`packages/components/src/styles/popupChromeScrim.module.css`

---

## 业务 / Consumer 怎么用

| 优先级 | 做法 |
|--------|------|
| **1（推荐）** | 直接用 DS 组件：`EgSkid`、`EgToolBar`、带 Paginer 的列表壳 — **禁止**自造顶底滚动结构 |
| **2** | 自定义面板：复制 **DOM 定稿 + 状态机**；挂 `.eds-frosted-page-chrome`；消费 Desktop token |
| **3** | 仅改颜色：只动 `--effect-mask` spec；**禁止**改 sticky 结构 / 滚动容器层级 |

**Consumer 硬约束（work-cregis-desktop 等）：**

- 100% Desktop token（`.desktopTokens` 或客户端 shell 已挂 token + `@eds/desktop-components/style.css`）
- Skid 场景：`<EgLayout #skid>` + `<EgSkid>` default slot；**禁止**外包自定义 fixed header
- 不在 consumer `src/**` 写 `backdrop-filter` / 不透明顶栏背景覆盖 DS
- 需新壳 → 回 **eds-desktop** 提组件或 composable，禁止在业务仓库抄 DOM

**Showcase 验收：** `/components/skid` — 勾选「显示测试内容」→ 长文本溢出 → 下滚后顶栏 / 未到底时底栏出现毛玻璃与分隔线。

---

## 常见失败原因（排查清单）

| 现象 | 原因 |
|------|------|
| 完全没有模糊 | 滚错容器；或未滚动（顶栏在顶部仍为实色） |
| 只有色块、不糊 | `background` 不透明；或 `--effect-mask` / `--eds-blur-bg` 未加载 |
| 模糊错层 / 失效 | 祖先 `transform` / `filter` / `opacity < 1` 新建 stacking context |
| 永远不触发 mask | `height: auto` 无溢出；`canScroll === false` |
| DS 已挂 class 仍实色 | 模块 CSS `box-page` 盖住；缺 `:global(.eds-frosted-page-chrome)` 覆盖 |

---

## Agent 禁令

- **禁止**在 consumer 为修毛玻璃改 `packages/**` 以外绕过 DS（须回 eds-desktop）
- **禁止**手搓 `position: fixed` 顶底栏替代 sticky-in-scroll（除非用户明确要求另一种交互）
- **禁止**私造 mask 色 / blur 字符串；须 `var(--effect-mask)` + `var(--eds-blur-bg)`
- 用户只说改 mask **不透明度** → 只改 `packages/tokens/spec/color/semantic.json` 中 `effect-mask`；**禁止**顺带改 Skid layout

---

## 参考文件

- `packages/components/src/templates/skid/Skid.vue` + `Skid.module.css`
- `packages/components/src/styles/frostedPageChrome.css`
- `packages/components/src/styles/popupChromeScrim.module.css`
- `packages/components/src/organisms/tool-bar/ToolBar.vue`
- `packages/components/src/organisms/paginer/Paginer.vue`
- `apps/showcase/src/views/components/previews/SkidPreview.vue`（溢出测试 demo）
- `apps/showcase/src/composables/scrollContainment.ts`（`SCROLL_EDGE_EPSILON`、scroll metrics）
