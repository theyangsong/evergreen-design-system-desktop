---
description: Stroke-based SVG icons — non-scaling stroke when sizing with --icon-* tokens
globs: packages/**/*,apps/showcase/**/*
alwaysApply: false
---

# 描边型 SVG 放大：边框不随缩放变粗

完整方案见 `packages/components/docs/icon-stroke-scaling.md`。

## EgIcon（token 线稿）

- `processSvg`：path 打 `eds-i-s`，**保留**源稿 `stroke-width="1.4"`，只剥 token 色值
- `Icon.module.css`：

```css
.tokenKind :global(.eds-i-s) {
  stroke: currentColor;
  stroke-width: var(--stroke-lg, 1.4px);
  vector-effect: non-scaling-stroke;
}
```

- `1.4px` fallback：Showcase Pages 等 scoped token 未继承时仍保持精准描边
- **不要**用 ResizeObserver / 运行时反算；**不要**去掉 inline `stroke-width` 后只靠 CSS

## Showcase 手写 SVG（非 EgIcon）

```css
.myIcon :global(path) {
  fill: none;
  stroke: currentColor;
  stroke-width: var(--stroke-lg);
  vector-effect: non-scaling-stroke;
}
```

`vector-effect` **不继承**，每个 path/line 须单独设。

## Toggle 外环

```css
border: none;
box-shadow: inset 0 0 0 var(--stroke-lg) var(--stroke-base-quaternary);
```
