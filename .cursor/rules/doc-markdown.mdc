---
description: Doc-site markdown rendering — frozen prose/code/table rules; do not re-tune per document
globs: apps/website/src/markdown/**,apps/website/src/content/docs/**/*.md,apps/website/src/views/DocPageView.vue
alwaysApply: false
---

# Doc Markdown (冻结)

**Do not** add per-page or per-document markdown CSS in `DocPageView.module.css`. All prose rules live in one module.

## Frozen decisions (session checkpoint)

| Area | Rule | Do not |
|------|------|--------|
| Lists | Top-level **and** nested `li + li` → `margin-top: spacing-4` | Revert nested to `spacing-1` without explicit request |
| Table `th` | bg `material-card-shallow`; border `stroke-outline-deep` | Use `moderate` / `subtle` for header bg |
| Table cells | `height: 38px`; `padding: spacing-2 spacing-3` | Change per-doc |
| Code blocks | Chrome + highlighted `pre`; **no line-number gutter** | Re-add line nums (reverted) |

## Single source of truth

| Concern | File |
|---------|------|
| Render pipeline | `apps/website/src/markdown/renderMarkdown.ts` |
| Prose + code styles | `apps/website/src/markdown/docMarkdown.module.css` |
| Copy button handler | `apps/website/src/markdown/useDocMarkdownCopy.ts` |
| Public API | `apps/website/src/markdown/index.ts` |

Usage in views:

```ts
import { docMarkdownStyles, renderMarkdown, useDocMarkdownCopy } from '@/markdown';
```

```html
<div :class="docMarkdownStyles.markdownBody" v-html="html" @click="handleMarkdownBodyClick" />
```

## Renderer rules (`renderMarkdown.ts`)

- GFM on; `breaks: false`
- `h1`: strip leading `N.` from title text; map `id` via `sectionIdsByTitle`
- Fenced code **with** lang → `docs-code-block` + chrome (lang label + copy) + `docs-fence-highlighted` + hljs
- Fenced code **without** lang → `docs-fence-plain` (ASCII diagrams)
- Image shorthand: line `!filename.png` → color-system asset path
- Supported langs: js/ts/tsx/json/css/bash (+ aliases)

## Typography (frozen)

| Element | Rule |
|---------|------|
| Body | `typography-body-medium`, `text-rendering: auto` |
| `h1` | `scale-8`, weight 700; top margin `scale-20` (0 if first) |
| `h2` | `typography-title`; margin `spacing-7` (`spacing-6` after h1) |
| `h3` | `typography-body-medium`, weight 500 |
| `h4–h6` | `typography-body-large-strong` |
| `strong` | weight 500 (not 700) |
| Block margin | `p/ul/ol/blockquote/pre/table/hr` → `spacing-4` top |

## Lists (frozen)

- Indent: `ul, ol` → `padding-left: spacing-6`
- **Top-level** — `.markdownBody > ul > li + li`, `.markdownBody > ol > li + li` → `margin-top: spacing-4`
- **Nested** — `.markdownBody li ul li + li`, `.markdownBody li ol li + li` → `margin-top: spacing-4` (same as top-level)

## Inline code (frozen)

- Font: IBM Plex Mono 14px
- Padding: `spacing-05` `spacing-1-5`; bg `material-card-moderate`
- Light: `#006e41` + `color(display-p3 0 0.4314 0.2549)`
- Dark: `#69b091` + `color(display-p3 0.41 0.69 0.57)`

## Fenced code blocks (frozen)

**Chrome** (`docs-code-chrome`): padding `spacing-4 spacing-3 spacing-3 spacing-5`
- Lang label: EDS Text (`font-family-text`), `typography-body-medium`
- Copy: 14px icon, `spacing-1` padding; green check on copy

**Highlighted pre** (inside block): padding `spacing-4 spacing-8 spacing-8` — **no line numbers**
**Plain pre**: padding `spacing-4`; bg `material-card-subtle`
**Font**: IBM Plex Mono 14px / 20px lh; `pre-wrap`

Syntax colors: hex fallback + display-p3 canonical pairs in `docMarkdown.module.css` — do not use global CSS or separate theme files.

## Blockquote (frozen)

- `border-left: stroke-xl solid stroke-divider-page`
- Inner gap: `spacing-1` (flex column)
- Color: `text-base-secondary`

## Table (frozen)

- `th, td` — `box-sizing: border-box`; `height: 38px`; `padding: spacing-2 spacing-3`; `vertical-align: middle`; `text-align: left`
- Border (all cells): `stroke-xs solid stroke-outline-deep`
- `th` only — `background: material-card-shallow`; weight `typography-body-medium-strong`
- `td` — no background override (inherits page)

## HR (frozen)

- `display: none` — markdown `---` separators are not shown

## When adding new docs

1. Write plain `.md` under `src/content/docs/` — **no** inline HTML for styling
2. Wire content in `navigation.ts` only
3. If a **new element type** is needed site-wide, extend `docMarkdown.module.css` + `renderMarkdown.ts`, not `DocPageView`
