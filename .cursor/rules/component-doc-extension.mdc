---
description: Showcase 组件文档五层扩展交付模型（Variants → Props → Slots → Composition → Scenarios）
globs: apps/showcase/src/views/components/previews/**,apps/showcase/src/views/shared/componentDoc/**
alwaysApply: false
---

# 组件文档扩展交付（五层模型）

## 五层定义

> **Variant ≠ Scene**（`evergreen-ia.mdc`）：文档 **Variants** 层 = 本体定制区配置项；侧栏 **Scenes** 子路由 = 文档 **Scenarios** 层（独立预览页）。禁止把 IconButton、DialogSymbol 等标为 `navRole: variant` 侧栏页。

| 层 | 含义 | Showcase 落地 |
|----|------|----------------|
| **Variants** | 类型 / 变体 / 尺寸矩阵 | 定制区 `type` / `variant` / `kind` 等下拉 |
| **Props** | 公开 props + 事件表 | `prop-rows` + `event-rows` |
| **Slots** | 公开插槽（无则文档注明） | `slot-rows`；无插槽写 `(无公开插槽)` 行 |
| **Composition** | 嵌套子组件定制 | `#customize-extra` / `#customize-after` 嵌套 `CustomizePanel` |
| **Scenarios** | 业务场景预设 | 定制首项 `scenario` + `apply*Scenario()` |

## 最低交付（按层级）

| 层级 | 必填层 |
|------|--------|
| atom | Props, Slots |
| molecule | Variants, Props, Slots |
| organism | Variants, Props, Slots, Composition |
| template | Variants, Props, Slots, Composition |
| scenes | Variants, Props, Scenarios |

实现：`extensionDelivery.ts` + `ExtensionDeliveryMatrix.vue`；`ComponentDocLayout` 传 `doc-tier`。

## 范本页（须对齐）

- **Dialog** — `/components/dialog-{standard,symbol,compose}`：Variants + Composition + slot-rows（Molecule 级）
- **Detail** — `/components/detail`：Composition（Section / Toolbar 嵌套面板）+ slot-rows
- **Flotation** — `/components/flotation`：Variants + Composition（Trigger / Menu / Box 多面板）

## 新组件 / 补文档 Checklist

1. 选定 `doc-tier`（atom / molecule / organism / template / scenes）
2. 补齐该 tier **必填层**（见上表）
3. `ComponentDocLayout` 传 `doc-tier`；扩展矩阵显示「待补」项须在本 PR 或跟进 issue 关闭
4. Popup **外壳页**不得承载 Dialog / Detail / Verify **内容**定制（§7 eds-project）
5. Showcase 文案：Scenes / Mode / Icons；API slug 可暂保留历史拼写（如 `popovers-scens`）

## 产品名 ↔ API（Dialog 域）

| 产品名 | 组件 | Popup |
|--------|------|-------|
| Dialog | `EgDialog` | `uses="dialog"` + `dialogType` |
| Detail | `EgDetail` | `uses="detail"` |
| Verify | `EgVerify` | `uses="verify"` + `verifyType` |

废弃：`EgReminder`、`uses="reminder"`、`reminderType`（仍 re-export 一版，勿在新代码使用）。
