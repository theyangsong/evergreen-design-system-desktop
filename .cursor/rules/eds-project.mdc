---
description: EDS Desktop 项目约定 — token、Showcase、同步、Organism 场景化
globs: packages/**/*,apps/showcase/**/*
alwaysApply: false
---

# EDS Desktop — 项目约定

> 专项规则：`display-p3-color-usage.mdc`（颜色）、`corner-smoothing.mdc`（圆角）、`stroke-svg-scaling.mdc`（SVG 描边）。
> 索引：**§0** 执行范围 · **§1** token · **§2–4** Showcase 命名/文案/演示数据 · **§5** 库作用域/HMR · **§6** 滚动边界 · **§7** Organism（含 Popup/Popover/Detail/Reminder 边界）· **§8** 同步。

## 0. Agent 执行范围

- 只做用户明确要求的事；**最小 diff**；禁止思维扩散（顺带改 layout、API、consumer、文档、测试）。
- 不确定时先问；用户说「同步」「生成数据」「一并」等 → 见 **§8**、**§4.4**。
- 无 token / 无 prop 枚举 → **停改**，先问是否允许硬编码。

| 用户说 | ✅ | ❌（除非另说） |
|--------|-----|----------------|
| 表头溢出也用 `…` | 表头 `text-overflow: ellipsis` | 改 flex、tooltip、combo 布局 |
| 同步 | §8 全流程 | 改 consumer 业务、自动 commit |

## 1. Token 与样式

**作用域**：Desktop 组件 + `.desktopTokens` → Desktop token；Showcase **壳层** → `@eds/website-tokens`（见 §5）。

**核心**

1. **语义优先**：先用途再选 token；查 `packages/tokens/spec/**` 与 `dist/css/**`。禁止因数值相同就用 `--scale-*` 代替 `--radius-*` / `--spacing-*` / `--stroke-*`。
2. **禁止私造 `--*`**、禁止局部 alias（`--foo: var(--radius-md)`）、禁止手拼 shadow/玻璃（用 `--eds-shadow-light`、`.effect-*`、`.eds-shadow-*-stack`）。
3. **Motion**（Base → Recipe → Semantic → DS glue）：
   - **base**（`@eds/desktop-tokens/motion/base`）：primitive only（`--eds-motion-*`、opacity/scale/offset 物理量）。**禁止**场景名。
   - **recipe**（`@eds/desktop-tokens/motion/recipe`）：duration role + easing + property 组合（`--motion-recipe-*`）。**仅 DS glue**；业务用 semantic。
   - **semantic**（`@eds/desktop-tokens/motion/semantic`）：**一个完整场景的默认动效组合** — **Ease**、**Layout**、**Flotation**、**Deform**、**Page**（如 `.motion-page { transition: var(--motion-recipe-page-push) }`）。只回答「这类场景用什么动效」，不回答「框架里怎么切页」。
   - **utilities**：`.motion-none`（focus/selected）。
   - **DS glue**（`packages/components/src/styles/*` 等）：框架 lifecycle（Vue `enter-from` / `leave-to`）、叠层布局、方向状态、`host` marker。**禁止**写入 `spec/motion/semantic.json`。
   - **Semantic 禁止**：`enter-from` / `leave-to` / `enter-active`、`host*`、direction modifier（`is-next` 等）、任何框架 lifecycle class名。
   - DS glue 示例：`overlayGlassMicroFloat.module.css`、`motionPageTransition.module.css`（`.motion-page-stack` + `data-page-direction` + Transition `name="motion-page"`）。

**Overlay 微浮动（microFloat）— 毛玻璃不失效**

- **核心**：`opacity` / `transform` 必须与 `backdrop-filter` 同在 **glass 自身**（`.effect-flotation-box` / `.effect-popup-box`），**禁止**挂在 glass 的父级（含 `.floatingInner`、`.eds-popover` 根、`.motion-micro-float` 包一层 children）。
- **实现**：组件内 overlay glue 用 `overlayGlassMicroFloat.module.css`（recipe/base 变量 + host active）；业务侧挂 semantic `.motion-flotation`（host `.is-active` / `.glassMicroFloatHostActive`）。
- **Popover（EgPopover）— 视觉锁死**
  - 默认宽 `POPOVER_PANEL_W = 336`（`popoverShape.ts`），高度自适应；用户只说改 shadow/颜色时 **禁止**动 layout / Vue / ResizeObserver。
  - `.shapeSurface`：挂 `effect-popover-box` + `backdrop-filter`（`--eds-blur-bg`）；**禁止**用 `effect-flotation-box`。
  - SVG 填充复刻 popover 栈：`.shapeFill` → `var(--effect-popover-box)`；`.shapeTint` → luminosity（`material-same-white-quaternary`）。
  - **阴影只挂 SVG `.outline` 的 `filter`**（避免左右分裂）：`drop-shadow(… stroke-base-tertiary)` + `drop-shadow(… --effect-vulvar-shadow-deep)`；**禁止**绑 `eds-shadow-light` / `effect-popup-background`；**禁止** `.shell` 挂 `filter`。
- **Flotation / List Field**：`EgAnchoredTooltip` microFloat 常驻 Teleport；入场 `visibility:hidden` 定位后再 reveal；离场不卸载 DOM、不用 `visibility:hidden` 收尾。
- **禁止**：为修闪烁去掉 opacity 动画；microFloat 路径每次 hover `v-if` 销毁 Teleport；改 `packages/tokens/spec/motion` 的 microFloat 参数（除非用户明确要求）。
- **详案**：`packages/components/docs/overlay-microfloat-flash-fix.md`。
4. **Typography**：整组角色 token（size+weight+line-height）；`font-family` 仅在 `global.css` 的 `body`/`.desktopTokens`；组件只绑 size/weight/line-height。
5. **Bar 11px**：`.eds-bar-text-subpixel` 或 2×+scale；NavBar 用 `.moduleLabel`/`.moduleLabelPaint`/`.moduleLabelSizer`（§7）。
6. **顶边距 %**：`height: var(--padding-top-*)` 或 flex 占位；**禁止** `padding-top: …%`。
7. **数字展示**：`formatGroupedNumber()`（展示层）；动态数字区加 `tabular-nums`。不适用：输入态、纯文案。

**常用映射**

| 说法 | 用 |
|------|-----|
| 圆角 12 | `--radius-md` |
| 1px 边框 | `--stroke-sm` / `--stroke-xs` |
| 间距 12 | `--spacing-3` |
| 13px 常规 / 中黑 | `--eds-body-small-*` / `--eds-body-small-strong-*` |

**`.module.css` 速查**：radius→`--radius-*`；spacing→`--spacing-*`；border→`--stroke-*`；shadow→`--eds-shadow-*`；color→`--material-*`/`--text-*`/`--stroke-*`。

## 2. 维度命名（preview key / fixture）

`{component}_{style}_{tone}_{size}_{status}` — 5 段 snake_case，无第六段。DOM 锚点仍 kebab-case（`button-sizes`）。

- style：`solid`|`outline`|`text`；tone/size/status 与组件 API literal 一致。
- 例：`button_solid_brand_lg_disabled`

## 3. Showcase 多子类锚点

- `catalog.ts` `children[].id` = 预览页根 `id`（如 `button-text`）；内层 section 二级锚点。
- 新增子类：**先 catalog + 根 id，再 section**。
- 同族 **一条整合 import**；第一个 child 无 `previewGroupLabel`，第二个起分割线 + 标题（`getCatalogChildLabel`）。

## 4. Showcase 文案与演示数据

**文案**：章节标题中文；prop 取值标签「中文 + 英文 token」（维护于 `showcasePropLabels.ts`）；prop 名/类型/Eg* 标签英文；说明中文。

**§4.4 生成数据**（「生成数据」等）：仅临时 `*DemoCustomizeState` 或会话内 `Object.assign`；**不写** `*CustomizeDefaults`；不 localStorage；优先只改 `apps/showcase/**`。`:customize-defaults` 始终指向永久 defaults；刷新须恢复。

## 5. 库作用域 · HMR · Token Scope

**Website 壳**（布局、导航、demo 标签）：`@eds/website-tokens` / `@eds/website-components`（`--eds-headline-small-*`、`--text-base-*`、`--spacing-*` 等）。

**Desktop**（`@eds/desktop-*`）仅用于：组件实现；live 预览实例；`.desktopTokens` 包裹 subtree；`/tokens` 页。

**禁止**：壳层用 Desktop 排版 `--eds-*`（无档位）或 `--typography-*`；把 label/previewStack 包进 `.desktopTokens`；Desktop 组件 CSS 用 Website token。例外：壳层动效 `apps/showcase/src/motion/tokens.css`。

### 5.1 实时生效（`pnpm dev`）

| 改什么 | 机制 |
|--------|------|
| `packages/components/**` | Vite alias → 源码；**HMR**；默认 **不** `build:components` |
| `packages/tokens/spec/**` | `watchDesktopTokens` → rebuild dist → full-reload |
| 手动 `build:tokens` | watch `dist/**` 触发刷新 |

未生效时：查 HMR、token 是否在 dist、specificity、硬刷新；勿先说「要 build」。仅 build：用户要求 / 验 dist / consumer / HMR 失败。

**HMR 陷阱**：`.desktopTokens button { font: inherit }` → 字重须 `.md:global(.eds-button)`；子组件类名是哈希 → 用 `:global(.eds-icon-button > span)` 等。

### 5.2 `.desktopTokens` DOM

- ✅ 壳层在外；**单独 div.desktopTokens 包组件 subtree**。
- ❌ label 进 desktopTokens；❌ `class="desktopTokens"` 打在 Eg* 根上（PostCSS 选择器失效）。
- 组件样式：`desktop-components-scope.css` scoped 引入 `@eds/desktop-components/style.css`。

## 6. 嵌套滚动边界

带内部滚动的 Desktop 组件（NavBar、ModuleMenu、Layout 侧栏等）：滚到顶/底 **不** 链式滚动外层、不回弹。

- CSS：`overflow-y: auto` + `overscroll-behavior: none`（不用 `contain`）。
- JS 同容器：**wheel**（passive:false，边界 preventDefault）+ **scroll** 钳制 scrollTop + `SCROLL_EDGE_EPSILON=2`。
- 参考：`NavBar.vue` + `.moduleRegion`；onBeforeUnmount 必须 removeListener。

## 7. Organism Showcase 场景化

**模式**：`ComponentDocLayout` + 定制首项「场景化」下拉 — DS 本体（全定制）vs 业务 preset（如 Cregis，仅场景化）。`customize.scenario` 不进 usage snippet（`omitKeys`）。

**Nav Bar**：preset 在 `apps/showcase/src/presets/<domain>/`；声明式 kebab-case props；无 slot 时用 `moduleLabelN` 等索引 props。动效：**仅 hover 过渡**；focus/选中 `.motion-none`。Icon：模块 glyph 容器 `--icon-lg` → `EgIcon size="md"`；底栏 `--icon-md` → `size="sm"`。

**EgMessage 嵌套**：`provide(MESSAGE_PARENT_FOCUSED_KEY)`；`focusBackground: inherit|same-white`；Bar 11px 用 sizer/paint，不用 barTextSubpixel composes。

**EgPopup — 外壳 vs 内容（锁死）**

```text
EgPopup 外壳
├── 遮罩（Reminder/Verify：仅 .backdrop；Detail：.stageScrim + animated scrim）
├── 点击空白关闭 / Esc 关闭
├── 容器进出场（.motion-layout + shellKeepMounted / shellMotionActive，出场后再卸载）
└── 默认插槽 ← Detail / Reminder / Verify 等内容
```

- **外壳只做**：全窗遮罩、空白/Esc 关闭、进出场动效、按 `uses` 选容器尺寸（detail 880×620；reminder/verify 按 Info/Echo 宽）。**不做**标题/副文案/符号/按钮等内容定制。
- **内容各自负责**：`EgDetail` / `EgReminder` / `EgVerify` 在各自组件与文档页配置；Popup 页 **禁止**编辑内容文案或 Reminder 定制控件。
- **遮罩范围**：须铺满 **NavBar + 侧栏 + 主内容** 整块客户端。根为 `width/height: 100%`，只填满直接父容器——挂在列表页 subtree 会被裁切。
- **挂载**：业务 Popup host 放在**客户端 shell**（与 Layout 同级或 shell Teleport 目标）；**禁止**挂在列表页、ModuleMenu 内容区等业务 subtree。
- **Alert 舞台顶边距**：`--padding-top-lg`（30% flex 占位，§1「顶边距 %」）。
- **Showcase 职责对照**（定制控件只放对应页）：

| 页面 | 定制范围 |
|------|----------|
| `/components/popup` | 用途（detail/reminder/verify）、Popup Box 宽度（info/echo）、开关闭演示 |
| `/components/detail` | Detail 全文案、Tab、Section、Apply_Item 等 |
| `/components/reminder` | 类型、标题、副文案、符号、符号背景色、按钮区 |
| `/components/verify` | 验证方式（单/双/锁定）、状态、标题、副文案、倒计时、切换文案 |
| `/components/popovers` | placement/align 等；`:wrap-tooltip="false"` 须布尔（勿传 truthy 字符串） |
| `/tokens` | 与 spec/dist 1:1 的 token 展示 |

- **EgReminder**：Info / Echo；符号区 56px 圆 + `EgIcon size="lg"`；可用 `--eds-reminder-symbol-bg` / `--eds-reminder-symbol-color` 覆盖。危险/成功背景用 `--status-danger` / `--status-success`，图标 `--stroke-same-white-primary`。
- **EgVerify**：Popup Box 固定宽高随 `type`（邮箱 328×436、Google 328×412、交易/登录/锁定 328×416、PassKey 328×406、2FA 358×459），不可改；Showcase 仅切换验证方式。
- **Showcase**：文档预览可用静态 Popup Box 壳（无全屏 scrim）；完整 `EgPopup` 行为见 `PopupPreview.vue`。

**Flotation Combo — 必须用 `EgFlotation`**

```text
EgFlotation → #trigger (EgFlotationTrigger) + Menu props + items/#content → EgFlotationMenu → EgFlotationMenuItem
```

- 禁止手搓 AnchoredTooltip+菜单、禁止平行 trigger 解析；须用 `flotationDocCustomize.ts`：`resolveFlotationComboTriggerProps`、`resolveFlotationComboEgFlotationProps`、`buildFlotationComboNestedSnippet`。
- Showcase 页：`/flotation`（Combo）、`/flotation-trigger`（Trigger）、`/flotation-box`（Box）。

**Flotation Box 页（`/flotation-box`）**

- 架构：`FlotationBoxPreview` orchestrator → `EgFlotationMenu`（shell 由 `flotationBoxPreviewShell.ts`）→ 场景 SFC 或 `FlotationBoxStandardPreview`。
- 场景 **独立 customize 键**（`scene*`），不与标准 `item*` 混用；切换 boxKind 仅「进入场景化」时 `apply*Preset`。
- Shell：`list-scroll=true`、`show-add=false`、`eds-flotation-menu--box-doc`（shell padding:0，body spacing-1 inset）。
- 禁止 Showcase 手搓 Tooltip/scroll-fade；禁止 `#header` slot + v-if 控 Tab。

**EgFlotationMenu 滚动（严格 — 见 `FlotationMenu.vue` + `Flotation.module.css`）**

- 自滚：`scrollable && (showAdd || listScroll)` → `eds-flotation-menu--scrollable-list`；面板 `scrollable=false`。
- Flex 链不断：`min-height:0` 贯穿 menu→panel→menuBody→menuListOuter→menuListScrollable。
- Padding：box-doc shell **padding:0**；inset 在 `.eds-flotation-menu-body`；列表区不加 padding-block。
- Scroll-fade：**仅** `menuListScrollable` 的 `mask-image`；有 Add 时禁 bottom fade；禁止 backdrop-filter 叠层 fade。
- sticky footer（showAdd）：panel overflow:hidden；列表末 `menuListScrollEnd`。

**Module Menu 场景化**：Cregis / UDun **独立** preset；其余回退 `moduleMenuComponentDefaultPresetGroups`；默认 `moduleBusinessTitle: Wallet`。标题：text / EgFlotation trigger（module-menu）/ Cregis WaaS·Payment Engine 浮层 — 见原表逻辑，preset 在 `presets/module-menu/`。

**EgModuleMenuItem 动效（锁死 — `moduleMenuMotion.ts` + `ModuleMenu.module.css`）**：**始终**挂 `.motion-ease.is-asym`（600ms 仅 `--motion-duration-hover-asym` / `eds-motion-5`）。悬浮入场有过渡、离场无；点击/聚焦/选中无过渡（CSS hover-only，不用 `motion-none`）。禁止 NavBar `is-hover` / `is-focus`。

**Data List 空态**：`eds-business-7`；不用 @cregis `EdsNoData`。

**EgDetail · Apply_Item（强约束 — 详案 `packages/components/docs/detail-apply-item.md`）**

Figma **Apply_Item@Cregis**（`2267:11092`）。标准 Detail 行 = **变体 id + 展示字段**；挂件（符号 / Tag / Link / Copy / 地址簿 / AML / 浏览器 / Title 图标组合等）由 DS catalog **锁死**。

| 角色 | 允许 | 禁止 |
|------|------|------|
| **业务** | `createDetailApplyItemRow(variantId, overrides)`；传接口展示字段 | 手拼 `DetailItemData` 并关挂件；CSS 隐藏；spread preset 后改挂件 |
| **DS** | 维护 `applyItemPresets.ts` 与 `EgDetail` 渲染 | 为单业务开 widget escape hatch |
| **Showcase** | preset 行仅切换「数据来源」 | merge customize 挂件字段；duplicate catalog |

- **业务必须**：`createDetailApplyItemRow`（`@eds/desktop-components`）。
- **仅可覆盖**：`key` | `title` | `value` | `tag` | `valueSymbolCrypto` | `valueIcon` | `valueSymbolAvatarName`。
- **Catalog 真源**：`packages/components/src/organisms/detail/applyItemPresets.ts`。
- **Showcase preset 行**：`createDetailApplyItemRow` / `resolveDetailItemFromApplyPreset` + `structuredClone`；切换数据来源时 reset 该行 customize 挂件字段。
- **非 catalog 自定义行**：文档/demo 例外，须 PR 说明；不得作常规详情页实现。
- **新增变体**：Figma 评审 → 更新 `applyItemPresets.ts` → export `DetailApplyItemVariantId` → 文档/Changelog。

```ts
// ✅ 业务
createDetailApplyItemRow('receiver', { value: addr, tag: alias })

// ❌ 业务
{ title: 'Receiver', value: addr, showValueCopy: false }
```

**参考文件**：`FlotationBoxPreview.vue`、`flotationBoxSceneAddressCustomize.ts`、`NavBarPreview.vue`、`moduleMenuPreviewCustomize.ts`、`detail-apply-item.md`、`applyItemPresets.ts`。

## 8. 本地同步

| 用户说 | 行为 |
|--------|------|
| 「同步」 | `pnpm build:tokens && pnpm build:components`（或 `pnpm sync`）→ **所有** link consumer `pnpm install` |
| 「同步到 X」 | 仅指定项目 |
| 「列出可同步项目」 | `pnpm sync:list`，不 build |

业务项目「同步 eds-desktop」= 先 build eds-desktop，再本项目 `pnpm install`。

**单向边界：** 同步只 **eds-desktop → consumer**（build、install、list-field 辅助复制）。consumer 的 `src/**` **不得**回写 `packages/**`。业务改 DS 须在开 **eds-desktop** 仓库进行。详见 `eds-sync-local-consumers.mdc`「单向边界」。

- 扫描 `~/Projects/**/package.json` 中含 `link:`/`file:` 指向 `eds-desktop/packages/{components,tokens}`；排除 monorepo 内 showcase/storybook。
- 已知 consumer：`work-cregis-desktop`（100% Desktop；禁 `@eds/website-*`、showcase 壳）。
- 同步含 list-field 辅助 ts 到 consumer；**不含** `listFieldCryptoSampleAddresses.ts`、showcase、website 包。
- 勿只 build 不同步 consumer；build 失败则停；不 push 除非明确要求。

## 9. 变更记录

- 2026-08-08：§1 Popover — `effect-popover-box` 填充栈；`.outline` shadow 用 `--effect-vulvar-shadow-deep`；禁误动 layout。
- 2026-08-08：§7 EgPopup — 外壳 vs 内容锁死；Showcase popup/detail/reminder/popovers 职责对照；Reminder 符号 token。
- 2026-08-07：§7 EgDetail Apply_Item — 变体 catalog + `createDetailApplyItemRow` 业务契约。
- 2026-08-07：§1 Motion 四层 — base primitive / recipe / semantic（is-active）/ utilities。
- 2026-08-07：§1 增补 Overlay microFloat 毛玻璃动效约束；共享 `overlayGlassMicroFloat.module.css`。
- 2026-08-07：全文精简；原分散 mdc 已并入 §4.4 / §5 / §8。
- 更早条目见 git history。
