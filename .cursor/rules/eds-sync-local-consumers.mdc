---
description: eds-desktop 本地同步约定 — 用户说「同步」时自动 build 并推送到所有引用本库的项目
alwaysApply: true
---

# eds-desktop — 本地同步约定

## 触发词

在 **eds-desktop** 仓库中，用户说以下指令时按约定执行：

| 用户说 | Agent 行为 |
|--------|------------|
| **「同步」** | 构建 eds-desktop，并同步到**所有**本地引用 `@eds/desktop-components` / `@eds/desktop-tokens` 的项目 |
| **「同步到 [项目名]」** | 仅同步到指定项目（如 `work-cregis-desktop`） |
| **「列出可同步项目」** | 扫描并列出可同步的本地 consumer，不执行 build |

在 **业务项目**（如 `work-cregis-desktop`）中，用户说 **「同步 eds-desktop」** 等同于：先在 `../eds-desktop` build，再在本项目 `pnpm install` 刷新 link。

## 单向边界（硬约束）

**目标模型：DS 改 → 业务实时跟；业务改 → 不污染 DS。**

| 方向 | 机制 | 允许 |
|------|------|------|
| **eds-desktop → consumer** | `link:` 读同一路径；vite alias 组件源码；dev 监听 `packages/tokens/spec` 自动 `build:tokens`；`pnpm sync` build + install + list-field 辅助 **复制到** consumer | ✅ |
| **consumer → eds-desktop** | 无反向 sync 脚本；业务 `src/**` 不写回 `packages/**` | ❌ 禁止 |

**Agent 在 eds-desktop 仓库：**

- 改 token / 组件 → 只动 `packages/**`、`apps/showcase/**`（DS 范围）；**禁止**把 `work-cregis-desktop/src/**` 的业务逻辑抄进 DS，除非用户明确要求提升为通用组件。
- 用户说「同步」→ 只 **向外** push build 产物与允许的 list-field 辅助文件。

**Agent 在业务 consumer（如 work-cregis-desktop）：**

- 业务 UI / 路由 / 场景 → **只改** consumer 的 `src/**`。
- **禁止**在 consumer 会话里改 `../eds-desktop/packages/**`，除非用户明确说「去改设计系统 / 改 eds-desktop」。
- 需要 DS 能力时：到 **eds-desktop** 仓库改，或请用户切换 workspace；不要在业务项目里「顺手」改 sibling DS。

**说明：** `link:` 共用磁盘，无法从技术上阻止人手改 sibling 目录；边界靠 **仓库职责 + Agent 规则**  enforce。业务 dev 的 `predev` 仅 **触发** eds-desktop `build:tokens`（读 spec 生成 dist），不会把业务代码写进 DS。

## 同步步骤（默认全流程）

1. 在 eds-desktop 根目录执行：
   ```bash
   pnpm build:tokens && pnpm build:components
   ```
   或 `pnpm sync`（若已配置）。
2. 发现本地 consumer：`pnpm sync:list` 或运行 `node scripts/sync-local-consumers.mjs --list`。
3. 对每个 consumer 执行 `pnpm install`（刷新 `link:` / `file:` 依赖）。
4. 汇报：已 build 的包、已同步的项目列表；若某项目 dev server 在跑，提示用户重启以加载新构建。

## 发现规则

扫描 `~/Projects`（eds-desktop 的上两级目录）下各项目的 `package.json`：

- `dependencies` / `devDependencies` 中含 `@eds/desktop-components` 或 `@eds/desktop-tokens`
- 且值为 `link:` / `file:` 指向 `eds-desktop/packages/*`

**排除** eds-desktop 自身 monorepo 内的 workspace 包（showcase、storybook 等由 `pnpm build` 已覆盖）。

## 当前已知 consumer

- `work-cregis-desktop` — **Desktop 客户端**；`link:../eds-desktop/packages/components` + `link:../eds-desktop/packages/tokens`；**禁止**引入 `@eds/website-*` 或 showcase 壳层配置

新 consumer 加入时无需改规则；发现脚本会自动扫描。若扫描不到（路径不在 `~/Projects`），手动指定「同步到 [路径]」。

## Desktop 客户端 consumer（如 work-cregis-desktop）

同步 **仅** 传播 `packages/tokens` 与 `packages/components` 的 build/源码，以及 **list-field-currency 运行时辅助文件**（`listFieldCurrencyShared.ts`、`listFieldCryptoResolve.ts` 等）到 `work-cregis-desktop/src/scenes/tasks/list-field/`。**不包含**：

- `listFieldCryptoSampleAddresses.ts`（work-cregis-desktop 维护业务本地扩展，sync **跳过**）
- `apps/showcase/**`（Website 壳 + Desktop 预览混合）
- `@eds/website-tokens` / `@eds/website-components`

客户端项目须 **100% Desktop**：排版与颜色只消费 `packages/tokens` spec 中已定义的语义 token；不得依赖 Showcase 外层 Website 变量继承。若 preview 与客户端视觉不一致，优先查 token 名是否在 Desktop spec 中存在，而非继续 sync Website 侧配置。

**Consumer 专项规则（不随 sync 复制，须仓库内维护）：** `work-cregis-desktop/.cursor/rules/top&botton-mask.mdc`（滚动顶底毛玻璃强推，`alwaysApply: true`）；真源 `eds-desktop/.cursor/rules/top&botton-mask.mdc`。

## Agent 注意

- 用户只说「同步」时，**不要**只 build eds-desktop 就结束；必须同步全部 consumer。
- 不要 push git，除非用户明确要求。
- build 失败则停止，不向 consumer 传播半成品。
