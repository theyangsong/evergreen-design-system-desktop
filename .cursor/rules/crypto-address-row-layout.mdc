---
description: EgCryptoCombo 双地址行布局契约 — Agent 禁止擅自改 DOM/flex/ellipsis；仅用户明确要求时可动
globs: packages/components/src/molecules/crypto-combo/**/*
alwaysApply: false
---

# CryptoAddress 双地址行 — 布局契约（锁死）

## Agent 禁令（最高优先级）

**未经用户明确要求，禁止修改**下列文件的 **布局 / DOM 层级 / flex·width·ellipsis / Flotation 拆分方式**：

- `CryptoAddressSide.vue`
- `CryptoCombo.module.css`（`.cryptoAddress*`、`.eds-crypto-address-*` 段）
- `CryptoAddressTags.module.css`

允许（无需用户点名）：纯文案、类型、 Menu 面板内容、复制逻辑、与布局无关的 bugfix。

**禁止**擅自做的「优化」示例（均已验收失败）：

- 把 `CryptoAddressSide` **根节点**改成 `EgFlotation`（会破坏 `.cryptoAddress` 子树 flex / 引发二次 ellipsis）
- 整侧一体 `EgFlotation`（地址 + `(N)` + Tag 合并 trigger）
- 去掉 `EgTextOverflowTooltip` 改裸 `<span>` + 自写 overflow 检测
- 改 `flex: 1 1 auto` / `width: 100%` 撑满半列
- 给已 JS 6…6 的地址再加 CSS `text-overflow: ellipsis`（二次省略）

> **Showcase 验收点**：List Field · Currency 预览  
> `ZEC Base  t1g722...JKnQfK Risk  →  t1g722...JKnQfB Custom +2`  
> 地址 / `(N)` / Tag 之间 **仅 `--spacing-1`**；无半列空白、无孤立 Custom 3px 竖条。

## Side Menu 交互（内嵌 Flotation — 不改 `.cryptoAddress` 子级 layout）

当 `showSideMenu` 时：

- **根节点仍是** `span.cryptoAddressSide`（`.cryptoAddress` 的直接子级不变）
- **一个** `EgFlotation` **嵌套在** `.cryptoAddressSide` **内部**（勿把 Flotation 提到组件根）
- `.cryptoAddressSide` = `eds-hover-tooltip-trigger`；`.cryptoAddressLine` = `__target`（虚线仅地址+(N)）；Tag 在 trigger 内但不在 `__target` 上
- 地址字段仍用 `EgTextOverflowTooltip`（`disabled` + 无 copy Menu；Side Menu 负责复制）

Plain 路径保持 `(N)` / Tag 各自 Flotation 的 6bef89d 布局。

## DOM 结构（定稿 — 勿改 `.cryptoAddress` 直接子级）

### Side Menu 路径

```
.cryptoAddress
└ .cryptoAddressSide / .eds-crypto-address-side   ← 组件根，永远是 span
    └ .eds-crypto-address-flotation               ← 内嵌，非 .cryptoAddress 直接子级
        └ trigger: .cryptoAddressSideMenuTrigger
            ├ .cryptoAddressLine.__target         ← 地址 + (N)
            └ .cryptoAddressTags                  ← Tag，不参与 __target
```

### Plain 路径

```
.cryptoAddressSide
├ .cryptoAddressLine → EgTextOverflowTooltip + (N)
└ .cryptoAddressTags / EgFlotation
```

Hook 类 **`eds-crypto-address-flotation`**、**`eds-crypto-address-side`** 写在 `CryptoAddressSide.vue`；CSS 选择器必须用它们。

## 双地址模式 — 不变量（禁止违反）

| 节点 | 必须 | 禁止 |
|------|------|------|
| `.cryptoAddress:not(.cryptoAddressSingle) > .eds-crypto-address-flotation` | `flex: 0 1 auto` | `flex: 1 1 auto`（会 50/50 撑满留空） |
| 同上 `> span`（Flotation trigger） | `width: max-content` | `width: 100%` |
| `.cryptoAddressSide` / `.eds-crypto-address-side` | `width: max-content` | `width: 100%` |
| `.cryptoAddressLine` | `flex-grow: 0` | `flex: 1 1 auto`（拉宽行盒，Tag 被推到半列末尾） |
| `.cryptoAddressTags`、`.inlineTag` | `flex-shrink: 0` | 外层 `overflow: hidden` + `flex-shrink: 1` |

## 单地址模式 — 例外

`.cryptoAddressSingle` 下 flotation / trigger 可用 `width: 100%`、`flex: 1 1 auto` 撑满列宽。

## 地址展示

- 行内地址用 **`truncateAddressMiddle(address, 6, 6)`**（JS 中截断），勿改回整段 CSS tail ellipsis。
- 列宽不足时 **优先收缩 `.cryptoAddressLine`**（`flex: 0 1 auto` + `overflow: hidden`），Tag 不压成竖条。

## 改动的唯一入口

仅当用户 **明确** 要求改 Currency 地址行布局 / 间距 / Flotation 结构时，才可动上述文件；**整侧 hover 一体**若要做，须用户确认接受 DOM 方案后再动。

## 相关文件

- `CryptoCombo.module.css` — LAYOUT CONTRACT 注释块
- `CryptoAddressTags.module.css` — Tag 行内 flex 契约
- `CryptoAddressSide.vue` — 定稿 DOM + hook 类
