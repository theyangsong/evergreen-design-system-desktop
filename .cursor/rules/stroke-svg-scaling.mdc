---
description: Stroke-based SVG icons — calc stroke-width compensation (Chrome 541684117 fallback)
globs: packages/**/*,apps/showcase/**/*
alwaysApply: false
---

# 描边型 SVG 放大

完整方案与 **Chrome 153 验证清单** 见 `packages/components/docs/icon-stroke-scaling.md`。

## EgIcon（当前回退）

- **不用** `vector-effect: non-scaling-stroke` — [Chromium 541684117](https://issues.chromium.org/issues/541684117)，153 前回归
- calc：`stroke-width: calc(var(--eds-icon-stroke-screen) * 32 / var(--eds-icon-display-px))`
- **不用** `shape-rendering`
- 业务例外：`--eds-icon-stroke-screen: var(--stroke-md)` 等

## 153 后

按 `icon-stroke-scaling.md` 验证清单通过后，可评估切回 `vector-effect` 简化公式。
