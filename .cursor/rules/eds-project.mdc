---
description: EDS Desktop 项目约定（唯一真源）
alwaysApply: true
---

# EDS Desktop — 项目约定（唯一真源）

> **§0** 执行 · **§1** token · **§2–4** Showcase · **§5** 作用域/HMR · **§6** 滚动 · **§7** Organism · **§8** 同步 · **§10–16** 专项 · **§9** 变更

## 0. Agent 执行范围

- 只做用户明确要求的事；**最小 diff**；禁止顺带改 layout、API、consumer、文档、测试。
- 不确定先问；「同步」→ §8；「生成数据」→ §4.4。
- 无 token / 无 prop 枚举 → **停改**，先问能否硬编码。

| 用户说 | ✅ | ❌ |
|--------|----|----|
| 表头溢出也用 `…` | 表头 `text-overflow: ellipsis` | 改 flex、tooltip、combo |
| 同步 | §8 全流程 | 改 consumer 业务、自动 commit |

## 1. Token 与样式

**作用域**：Desktop 组件 + `.desktopTokens` → Desktop token；Showcase **壳层** → `@eds/website-tokens`（§5）。

1. **语义优先**：查 `packages/tokens/spec/**`、`dist/css/**`。禁止用 `--scale-*` 顶替 `--radius-*` / `--spacing-*` / `--stroke-*`。
2. **禁止私造 `--*`**、局部 alias、手拼 shadow/玻璃（用 `--eds-shadow-*`、`.effect-*`、`.eds-shadow-*-stack`）。
3. **Motion**（base → recipe → semantic → DS glue）：
   - **base**：primitive only（`--eds-motion-*`）。**禁止**场景名。
   - **recipe**：`--motion-recipe-*`。**仅 DS glue**。
   - **semantic**：`.motion-page`、`.motion-flotation`、`.motion-layout-deform` 等完整场景默认。**禁止** `enter-from`/`leave-to`/`host*`/方向 modifier。
   - **DS glue**：Vue lifecycle、叠层、方向状态。**禁止**写入 `spec/motion/semantic.json`。详案：`layout-deform-page-switch.md`、`overlay-microfloat-flash-fix.md`。
4. **microFloat**：`opacity`/`transform` 与 `backdrop-filter` **同在 glass**（`.effect-flotation-box` / `.effect-popup-box`）；glue `overlayGlassMicroFloat.module.css`；业务 `.motion-flotation`。
5. **EgPopover（视觉锁死）**：宽 336（`popoverShape.ts`）；只说改 shadow/颜色时 **禁止**动 layout。`effect-popover-box`（非 flotation）；阴影仅 SVG `.outline` filter。禁止为修闪烁去掉 opacity 动画或 hover `v-if` 销毁 Teleport。
6. **Typography**：整组角色 → `.typography-*`（`@eds/desktop-tokens/text/styles`）；单轴覆盖用 `--eds-*-size/weight/line-height`。
7. **其它**：顶边距用 `height: var(--padding-top-*)`（禁 `padding-top: %`）；数字 `formatGroupedNumber()` + `tabular-nums`；Bar 11px → `.eds-bar-text-subpixel` 或 NavBar sizer/paint（§7）。

| 说法 | 用 |
|------|-----|
| 圆角 12 / 间距 12 / 1px 边 | `--radius-md` / `--spacing-3` / `--stroke-sm` |
| 13px 常规/中黑 | `--eds-body-small-*` / `--eds-body-small-strong-*` |

顶底毛玻璃 → **§13**；优先 `EgSkid` / `EgToolBar` / `Paginer`。

## 2. 维度命名（preview key）

`{component}_{style}_{tone}_{size}_{status}` — 5 段 snake_case。DOM 锚点 kebab-case。例：`button_solid_brand_lg_disabled`。

## 3. Showcase 目录

顶栏：**Tokens · Animations · Components · Patterns · Workflows**（详 **§14**）。`/patterns` ≠ 组件侧栏 Scenes。slug / `Eg*` 不改。

- `catalog.ts` `children[].id` = 预览页根 `id`；新增：**先 catalog + 根 id，再 section**。
- 同族一条整合 import；第二 child 起加 `previewGroupLabel` 分割线。

## 4. 文案与演示数据

章节中文；prop 标签「中文 + 英文 token」（`showcasePropLabels.ts`）；prop 名/Eg* 英文。

**生成数据**：仅临时 `*DemoCustomizeState` 或会话 `Object.assign`；**不写** `*CustomizeDefaults`、不 localStorage；`:customize-defaults` 始终永久 defaults。

## 5. 库作用域 · HMR

| 层 | 用 |
|----|-----|
| Showcase 壳 | `@eds/website-tokens` / `@eds/website-components` |
| Desktop | 组件实现、`.desktopTokens` 包 subtree、`/tokens` |

**禁止**：壳层 Desktop 排版 token；label 进 `.desktopTokens`；`desktopTokens` 打在 Eg* 根上；Desktop CSS 用 Website token。

**HMR**：`packages/components/**` → alias+HMR；`tokens/spec/**` → watch rebuild+reload。未生效先查 HMR/dist/specificity，勿先说 build。

**Native reset**：禁 `font: inherit`（仅 `font-family: inherit`）；禁 `button:not(.eds-xxx)` 抬特异性；typography 用 `.class:global(.eds-*)`；`textarea` reset 排除 `.eds-textarea-control`。

## 6. 嵌套滚动边界

内部滚动组件（NavBar、ModuleMenu 等）：顶/底 **不**链式滚外层。`overflow-y: auto` + `overscroll-behavior: none`；JS：`wheel`（passive:false）+ scroll 钳制 + `SCROLL_EDGE_EPSILON=2`。参考 `NavBar.vue`。

## 7. Organism · Showcase 场景化

**模式**：`ComponentDocLayout` +「场景化」下拉；`customize.scenario` 不进 snippet（`omitKeys`）。

### EgPopup — 外壳 vs 内容（锁死）

外壳：全窗遮罩、空白/Esc、进出场、按 `uses` 定尺寸。**不做**标题/按钮等内容。内容：`EgDetail`/`EgDialog`/`EgVerify` 各自页配置。Popup host 挂**客户端 shell**（禁列表页 subtree）；遮罩须盖 NavBar+侧栏+主区。

| 页面 | 定制范围 |
|------|----------|
| `popup` | uses、dialog 类型、开关演示 |
| `detail` / `dialog-*` / `verify` | 各内容全文案与控件 |
| `popovers-popover` | EgPopover 方向矩阵 |
| `popovers-scens` | 引导/备注/矿工费场景 |

`EgDialog`：Symbol/Compose/Standard（`EgReminder` 废弃）。`EgVerify` 尺寸随 type 固定不可改。

### Popovers Scens

- 引导：`.popoversGuideHost` **包裹** `EgPopover`。
- 备注：`teleport-to="body"`；placement/align 变须 `updatePosition()`。
- 矿工费标记：`data-miner-fee-popover`（EVM/BTC，保留 spacing-3）| `data-miner-fee-ton-like` / `data-miner-fee-tron`（无左右缩进）。

### Layout Deform（Popover 多页 A↔B）

**禁止** `.motion-page` / `mode="out-in"` 替代。须 `useMotionLayoutDeformPageSwitch`；shell 高度 morph；内容 **+offset→0**；layout 绑 **`activePage`**。详案：`layout-deform-page-switch.md`。

### Flotation

- Combo 须 `EgFlotation` 链路 + `flotationDocCustomize.ts`；禁手搓 AnchoredTooltip 菜单。
- Box 页：场景 `scene*` 键独立；`eds-flotation-menu--box-doc`；scroll-fade 仅 `menuListScrollable` mask。
- `EgModuleMenuItem`：`.motion-ease.is-hover-enter-only`；禁 NavBar `is-hover`。

### EgDetail · Apply_Item（详案 `detail-apply-item.md`）

业务 **必须** `createDetailApplyItemRow(variantId, overrides)`；挂件由 `applyItemPresets.ts` 锁死。仅可覆盖：`key|title|value|tag|valueSymbolCrypto|valueIcon|valueSymbolAvatarName`。禁手拼 `DetailItemData`、CSS 藏挂件。

多地址：`addressLayout: 'multi-collapsed'` + `addressCount` + `valueEntries`；禁手拼 `address (N)`。`toolbarDividerPinned` 控制分割线，禁 CSS 强写。

### 其它

NavBar preset → `presets/<domain>/`；hover 过渡 only，选中 `.motion-none`。Data List 空态 `eds-business-7`。Module Menu：Cregis/UDun 独立 preset。

## 8. 本地同步

| 用户说 | 行为 |
|--------|------|
| 「同步」 | build eds-desktop → **所有** link consumer `pnpm install` |
| 「同步到 X」 | 仅指定项目 |
| 「列出可同步项目」 | `pnpm sync:list`，不 build |

**单向**：DS → consumer ✅；consumer `src/**` 回写 `packages/**` ❌。步骤：`pnpm sync`（或 build:tokens+components）→ sync:list → 各 consumer install → 汇报。

扫描 `~/Projects/**/package.json` 含 `link:`/`file:` 指向 `eds-desktop/packages/*`。`work-cregis-desktop`：100% Desktop；含 list-field 辅助 ts；**不含** `listFieldCryptoSampleAddresses.ts`、showcase、website。consumer 顶底毛玻璃真源 **§13**。勿只 build 不同步；build 失败则停。

## 10. Display P3

双声明：hex 仅 fallback；**canonical = `color(display-p3 …)` 行**。消费须 `var(--*)`；mix 用 `color-mix(in display-p3, …)`；禁组件内 sRGB hex 覆盖 token。

## 11. Corner Smoothing

全局 squircle **60%**（`--corner-smoothing: 0.6`）；`var(--radius-*)` 禁硬编码 px。`border-radius` 在视觉圆角元素上；禁 per-component clip-path（全局 scanner）。例外：`.effect-*-box` 四件套、opt-out `data-no-corner-smoothing`。动态 UI：`rescanCornerSmoothing(root)`。

## 12. SVG 描边（EgIcon）

禁 `vector-effect: non-scaling-stroke`。`stroke-width: calc(var(--eds-icon-stroke-screen) * 32 / var(--eds-icon-display-px))`。详案：`icon-stroke-scaling.md`。

## 13. Top & Bottom Mask

固定高面板内纵向滚动：顶/底 sticky + `var(--effect-mask)` + `var(--eds-blur-bg)`。单滚动容器；禁 fixed 顶底栏；禁不透明 scrim。状态：`SCROLL_EDGE_EPSILON=2`。优先 `EgSkid`/`EgToolBar`/`Paginer`；参考 `Skid.vue`、`frostedPageChrome.css`。

## 14. EverGreen IA

| 一级 | 路由 |
|------|------|
| Tokens / Animations / Components / Patterns / Workflows | `/tokens` … `/workflows` |

Components：Atoms / Molecules / Organisms / Templates；默认本体 + `navSection: Scenes`。**Variant ≠ Scene**（定制区 vs 侧栏 Scenes 子路由）。Flotation 例外：Trigger/Box/Combo `navSubgroup`。catalog：`catalogHelpers.ts`、`navSection`、`buildCatalogAnchorItems`。

## 15. 组件文档五层

Variants → Props+Slots → Composition → Scenarios。最低：atom→Props+Slots；molecule→+Variants；organism→+Composition；scenes→+Scenarios。`extensionDelivery.ts` + `ComponentDocLayout` `doc-tier`。Popup 外壳页不承载 Dialog/Detail/Verify 内容（§7）。

## 16. Crypto 双地址行（锁死）

**未经明确要求禁止改** `CryptoAddressSide.vue`、`CryptoCombo.module.css`（`.cryptoAddress*`）、`CryptoAddressTags.module.css` 的布局/DOM/ellipsis/Flotation 拆分。

不变量：`.eds-crypto-address-flotation` `flex: 0 1 auto`；trigger `width: max-content`；`.cryptoAddressLine` `flex-grow: 0`。地址 `truncateAddressMiddle(addr, 6, 6)`（JS，禁再加 CSS ellipsis）。

## 9. 变更记录

- 2026-08-24：规则整合为单文件；全文精简浓缩。
- 更早见 git history。
